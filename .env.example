# Admin access token — anyone with the URL ?admin=<token> can upload workout files.
# Set this in your Amplify environment variables (do not commit the real value).
VITE_ADMIN_TOKEN=change-me-to-something-secret
